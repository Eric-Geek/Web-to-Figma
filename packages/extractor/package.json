{
  "name": "@web-to-figma/extractor",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome Extension for extracting DOM structure and styles from web pages",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo 'no tests configured yet'"
  },
  "dependencies": {
    "@web-to-figma/shared": "workspace:*",
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
